#include <iostream>
#include <queue>
using namespace std;

int main()
{
    queue<int> s;

    s.push(10);
    s.push(20);
    s.push(30);

    while (!s.empty())
    {       
        cout << s.front() << endl;
        s.pop();
    }
    
    return 0;
}